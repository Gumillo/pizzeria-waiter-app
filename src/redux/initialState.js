const initialState = {
  tables: {
    data: [],
    loading: {
      active: false,
      error: false,
    },
    updating: {
      active: false,
      error: false,
    },
  },
};

export default initialState;
